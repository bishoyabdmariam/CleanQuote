import 'package:flutter/material.dart';

 
  class  Favourite_quoteWidget extends StatelessWidget{
  const Favourite_quoteWidget({super.key});

   @override
   Widget build(BuildContext context) {
     return Scaffold(
     body: Container(

      )
         );
    }  

 }