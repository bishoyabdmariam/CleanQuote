import 'package:flutter/material.dart';

 
  class  Random_quoteWidget extends StatelessWidget{
  const Random_quoteWidget({super.key});

   @override
   Widget build(BuildContext context) {
     return Scaffold(
     body: Container(

      )
         );
    }  

 }